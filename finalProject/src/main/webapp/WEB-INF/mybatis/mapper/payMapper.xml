<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="pay">
	<select id="listPay" parameterType="map" resultType="com.sp.pay.Pay">
		SELECT * FROM (
			SELECT ROWNUM rnum, tb.* FROM (
				SELECT g1.g1_Num, g1_Id, g1_Pwd, g1_Name, g1_Sleep, cat_Code,
				NVL(giupAd_Num, 0) giupAd_Num
				FROM giup1 g1
				LEFT OUTER JOIN giupAd ga ON g1.g1_Num = ga.g1_Num
				WHERE cat_Code = #{category} AND g1_Sleep = 1
				ORDER BY giupAd_Num DESC
<![CDATA[
			) tb WHERE ROWNUM <= #{end}
		) WHERE rnum >= #{start}
]]>
	</select>
	
	<select id="menuCateRead" resultType="map">
		SELECT menuct_Num, menuct_Title, menuct_Info FROM menuCate
	</select>
</mapper>