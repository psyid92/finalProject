<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="giupQna">

	<!-- 신규추가 -->
	<insert id="insertQna" parameterType="com.sp.giupQna.Qna">
		INSERT INTO giupQna(q_Num, q_Title, q_Content, q_OriginalFileName, q_SaveFileName, 
			q_GroupNum, q_Depth, q_OrderNo, q_Parent, g1_Num)
		VALUES(giupQna_seq.NEXTVAL, #{q_Title}, #{q_Content}, #{q_OriginalFileName}, #{q_SaveFileName},
			#{q_GroupNum},#{q_Depth}, #{q_OrderNo},#{q_Parent},#{g1_Num})
	</insert>
	
	<!-- sql 태그 -->
	<sql id="where-list">
		<if test="searchKey=='g1_id' ">
			g1_id=#{g1_id}
		</if>
		<if test="searchKey=='q_Title'">
			q_Title=#{q_Title}
		</if>
		<if test="searchKey=='q_Content'">
			q_Content=#{q_Content}
		</if>
		<if test="searchKey=='q_Created'">
			q_Created=#{q_Created}
		</if>
		
	</sql>	
	
	<!-- 글리스트, 글 개수 -->
	<select id="listQna" parameterMap="map" resultType="com.sp.giupQna.Qna">
		
	</select>	
	
	<select id="dataCount" parameterType="map" resultType="Integer">
	
	
	</select>
	
	<!-- 상세보기  : 조회수 증가 -->
	
	
	<!-- 이전글, 다음글 -->
	
	
	<!-- 글 수정  -->
	
	
	<!-- 글 삭제 -->
	
	
	<!-- 대댓글 넘버 증가 -->

</mapper>